'use strict';

/** Nombres de los eventos de dominio. */
module.exports = Object.freeze({
  HABIT_LOGGED: 'habit.logged',
  HABIT_CREATED: 'habit.created',
  LEVEL_UP: 'level.up',
  ACHIEVEMENT_UNLOCKED: 'achievement.unlocked',
});
